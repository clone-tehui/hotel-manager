import { BadRequestException, ConflictException } from '@nestjs/common';
export const OTA_CHANNELS = { AIRBNB: { channel: 'AIRBNB', displayName: 'Airbnb', credentialFields: ['partnerId', 'clientId', 'clientSecret'] } } as const;
export function normalizeOtaChannel(value: string) { const channel = String(value || '').trim().toUpperCase(); if (channel !== 'AIRBNB') throw new BadRequestException('Kênh OTA chưa được hỗ trợ'); return channel; }
export function assertSafeCredentialShape(value: unknown) { if (!value || typeof value !== 'object' || Array.isArray(value)) throw new BadRequestException('Thông tin xác thực OTA không hợp lệ'); const entries=Object.entries(value as Record<string,unknown>); if (!entries.length||entries.length>10) throw new BadRequestException('Thông tin xác thực OTA không hợp lệ'); for(const [key,item] of entries) if(!/^[a-zA-Z][a-zA-Z0-9_]{0,63}$/.test(key)||typeof item!=='string'||!item.trim()||item.length>4096) throw new BadRequestException('Thông tin xác thực OTA không hợp lệ'); return Object.fromEntries(entries.map(([key,item])=>[key,String(item).trim()])); }
export function connectorNotReady() { return new ConflictException('OTA connector chưa có adapter đã được phê duyệt; không thực hiện hành động bên ngoài'); }
